scalar Date

# スキーマ定義
type Query {
  info: String!
  feed: [Link]!
}

type Mutation {
  post(url: String!, description: String!): Link!
}

type Link {
  id: Int!
  createdAt: Date!
  description: String!
  url: String!
}
